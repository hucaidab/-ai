# ============ 密钥与敏感配置（禁止提交） ============
# 团队统一使用环境变量接入 LLM（见 LLM_CONFIG.md）
llm.config.json
*.local.json
.env
.env.*

# ============ 依赖 ============
node_modules/

# ============ 运行日志 / 临时文件 ============
*.log
tmp/
.tmp/

# ============ 本地环境（个人/可再生成，不进团队仓库） ============
# 个人工作记忆不进仓库；项目级技能 .workbuddy/skills/ 需要进仓库（团队 WorkBuddy 共享）
.workbuddy/memory/
llm-verify-out/
