# 流程图设计智能体 V5 迭代计划（对标专业流程图软件）

> 目标：把 V4 的"一次性生成器"升级为"可对话的设计助手"，并对标专业流程图软件（draw.io / ProcessOn / Visio / Lucidchart / 亿图）补齐能力。
> 三线并行：**主提升级线（P0/P1/P2 + 对标 A/B 档）‖ A1 可编辑画布线（架构级）‖ C 档规模化线（收尾）**

| 项 | 内容 |
|---|---|
| 版本 | V5（迭代计划，待逐批确认） |
| 日期 | 2026-08-14 |
| 前置 | V4 全链路已通（建模→拆图→渲染→验收→预览），edit-flow.mjs 多轮修改原型已验证 |
| 依赖关系 | 批1（协议+对话能力）→ 批2（图型+导入导出）→ 批3（AI 建模质量）；A1 画布线自批1起并行；批5 收尾 |

---

## 迭代总览

| 批次 | 主题 | 内容 | 交付物 | 验收 |
|------|------|------|--------|------|
| **批1** | 对话化基础（P0） | 多轮修改 / 补全引导 / 自动修复 | edit-flow 正式版 + repair 模块 | 15 用例 |
| **批2** | 多图型 + 导入导出（A2/A3/B3） | ER / 时序 / 用例图 + PNG/PDF/Word + Mermaid 导入 | 3 图型管线 + 导出模块 | 每图型 3 用例 |
| **批3** | AI 建模质量（P1+B1+B2） | few-shot 动态化 / 语义匹配 / 结构化覆盖 / 追问 / PRD 提取 | llm-model 重构 + prd-extract | 10 用例 |
| **批4** | 可编辑画布（A1，并行主线） | HTML 画布拖拽 / 改文字 / 改连线 / 存回 req.json | editor.html + canvas.mjs | 编辑后重渲染一致 |
| **批5** | 工程与规模化（P2+C） | 验收解耦 / 文档统一 / 模板 200 / 资产库 / 版本评审 | validate 重构 + assets 模块 | 全量回归 |

---

## 批1 · 对话化基础（P0 三项）⭐ 起点

**产出**：

1. **`edit-flow.mjs` 正式版**（原型 → 完整指令集）：
   - 改名类：角色/部门/标题/动作文本改名
   - 结构类：插入判断、插入步骤、删除节点、加角色/加泳道
   - 样式类：逆向边改红色虚线、节点换色、判断标签改名
   - 每条指令输出 `applied[]`（做了什么）+ `notApplied[]`（没识别什么）
2. **补全引导模块**（P0-2）：建模失败 → 输出"缺哪几类信息 + 参考示例"清单，不冷冰冰报错
3. **自动修复模块**（P0-3）：验收不过 → 自动补起止节点/补判断出口标签/补泳道 → 重跑验收，3 次内收敛，修复过程写进 report

**验收（15 用例）**：
- 多轮修改 5 例：改名 / 插入判断 / 删除节点 / 改样式 / 复合指令
- 补全引导 5 例：缺角色 / 缺判断 / 缺逆向 / 无信息 / 模糊需求
- 自动修复 5 例：缺 start / 缺 end / 判断缺标签 / 泳道重叠 / 多问题组合
- 全部产出 12/12 验收通过

---

## 批2 · 多图型 + 导入导出（A2/A3/B3）

**产出**：

1. **ER 图**（实体/属性/主键/关系，关系线带基数 1:N / M:N）
2. **时序图**（参与者生命线/激活条/消息箭头/返回虚线）
3. **用例图**（参与者/用例椭圆/系统边界/关系）
   - 复用现有「解析→布局→SVG」管线，新增 shape + 布局模式，DSL/LLM 建模扩展对应语法
4. **多格式导出**：PNG（高倍缩放）/ PDF / 嵌入 Word、PPT（生成可插入文件）
5. **Mermaid 导入**（B3）：粘贴 Mermaid flowchart 代码 → 解析 → 出 SVG（reqToMermaid 已输出，补反向）

**验收**：每个图型 3 个真实需求用例，出图+验收全通过；导出格式逐一打开验证。

---

## 批3 · AI 建模质量（P1+B1+B2）

**产出**：

1. **few-shot 动态化**（P1-1）：LLM 调用前先用 template-finder 命中相似模板，把模板塞进 few-shot 示例（代替硬编码单例）
2. **模板语义匹配**（P1-2）：同义词表（进货=采购、报账=报销…）+ 组合需求拆解
3. **结构化变量覆盖**（P1-3）：支持"加个部门/删掉第 3 步/审批链改成两级"等结构化修改
4. **追问式生成**（B1）：信息不足时先问（对标 DiagramGPT），一轮追问后出图
5. **PRD 文档提取**（B2）：粘贴 PRD/需求文档正文 → 提取业务流程 → 出图（乙方独有场景）

**验收**：10 个自然语言用例（含复杂流程）直接出图 12/12；无 key 时降级链可用；PRD 提取 3 例。

---

## 批4 · 可编辑画布（A1，并行主线）⭐ 架构级

**产出**：`editor.html` + `canvas.mjs`

- 把生成的 SVG 变成**可编辑 HTML 画布**：拖拽节点位置、双击改文字、改样式（颜色/虚线）、增删节点、重连边
- **单一数据源**：所有编辑写回 req.json → 重新渲染 → 验收（与批1 的 edit-flow 共享协议，永不双写）
- 编辑后一键导出 PNG/PDF（对接批2 导出模块）

**验收**：编辑后重渲染与手改一致；req.json 回写无损（diff 验证）；拖拽不重叠。

---

## 批5 · 工程与规模化（P2+C）

**产出**：

1. **验收解耦**（P2-1）：validate 不再硬编码渲染细节（去掉 y=50 正则），检查项从渲染上下文取数
2. **文档口径统一**（P2-2）：SPEC / SKILL.md / README / 模板数四者对账
3. **模板库规模化**（C1）：50 → 200+（覆盖甲方常见行业，gen-more-templates 批量生成）
4. **流程资产库**（C2）：按项目归集图 + req.json，支持检索/复用
5. **版本历史 + 评审**（C3）：每次修改留版本快照（diff），甲方评审留确认痕迹

**验收**：全量回归（batch 全部模板）+ 口径一致性检查脚本。

---

## 依赖与并行关系

```
批1 对话化基础（P0）──────────────┬──→ 批2 多图型/导入导出 ──→ 批3 AI 建模质量
                                  │
批4 可编辑画布（A1，自批1起并行）──┘                            └──→ 批5 收尾（P2+C）
```

- 批1 定义**稳定的 req.json 协议 + 对话能力**，是批2/批3/批4 的共同地基
- 批4（画布）与批2/批3 无强依赖，可并行开发，但必须共享批1 的 req.json 协议
- 批5 依赖前面全部，最后统一收口

## 风险与对策

| 风险 | 对策 |
|------|------|
| 可编辑画布工程量大 | 批4 独立主线并行，不阻塞主提升级线；先做"拖拽+改文字"最小可用版 |
| 多图型布局复杂（时序图激活条等） | 每种图型独立 layout 模式，先 ER（最简单）→ 用例 → 时序 |
| LLM 输出多图型 JSON 不稳 | 图型字段加 schema 校验 + 降级到 DSL/模板 |
| PRD 提取不准 | 先段落级提取 + 人工确认流程片段，再合并成图 |
| 模板 200 个质量不均 | 批量生成后全量 agent-batch 验收，不过的剔除 |
