# 流程图设计智能体 V4 深化计划

> 目标：把 V4 基础版升级为"能直接用自然语言出图"的完整智能体。
> 三个方向：**① LLM 自然语言建模 / ② 自动拆图 / ③ 知识库模板**

| 项 | 内容 |
|---|---|
| 版本 | v1.0（待确认） |
| 日期 | 2026-08-14 |
| 前置 | V3 自研管线 + V4 基础能力（nlp-model / agent-batch / preview-server）已可用 |
| 依赖关系 | M1 → M2（模板供拆图样例）→ M3（模板供 few-shot）→ M4；M1/M2 可并行 |

---

## M0 · 环境确认（阻塞项）

| 确认点 | 说明 |
|---|---|
| LLM API 配置 | ① 提供 base_url/api_key/model（deepseek 兼容 OpenAI 格式）；② 走环境变量 `LLM_API_KEY/LLM_BASE_URL/LLM_MODEL`；③ 暂无 key → 先跳过 LLM，用规则引擎版 |
| 执行顺序 | 推荐 **③知识库 → ②拆图 → ①LLM**（先建素材库，再拆图算法，最后上 LLM）；也可按 ①②③ |

---

## M1 · 知识库模板沉淀（方向③）

**产出**：
1. `templates/` 目录，8 个结构化模板（req.json 格式）：
   | 模板 | 触发词 | 角色 |
   |---|---|---|
   | 采购到付款 P2P | 采购/进货/供应链 | 采购员/采购经理/供应商/仓库/质检/应付会计/出纳 |
   | 销售订单 OTC | 销售/订单/收款 | 销售员/销售经理/信用专员/仓管员/应收会计/出纳 |
   | 员工入职 | 入职/招聘/HR | 候选人/HR/用人部门/IT/财务 |
   | 费用报销 | 报销/费用/差旅 | 员工/部门经理/财务/出纳 |
   | 请假审批 | 请假/考勤/审批 | 员工/直属经理/HR |
   | 登录认证 | 登录/认证/鉴权 | 用户/前端/认证服务/数据存储/MFA |
   | 生产领料 | 领料/生产/制造 | 生产计划/车间/仓库/质检 |
   | 退货退款 | 退货/退款/售后 | 客户/客服/质检/财务 |
2. `template-finder.mjs`：需求文本 → 触发词匹配 → 返回模板 + 变量替换（部门/角色/标题可按需覆盖）。

**验收**：8 模板经 `agent-batch` 批量出图 100% 通过；finder 触发词命中正确率 100%（8/8）。

---

## M2 · 自动拆图（方向②）

**产出**：`split-graph.mjs`
- 输入：req.json（或 mmd），节点数 > 阈值（默认 30）
- 算法：
  1. 识别关键路径（从开始到结束，按出入度 + 拓扑深度加权）
  2. 非关键路径的"子树/分支块"折叠为**子流程符号** `[[子流程名]]`
  3. 每个被折叠的子树单独生成一张子流程图（含自己的开始/结束与泳道）
- 输出：主图 SVG + N 张子图 SVG + `index.html`（图索引导航，主图↔子图互链）
- 每张图独立走 agent-batch 验收

**验收**：构造 40+ 节点示例（"订单全生命周期" = P2P + OTC + 生产领料合并）→ 自动拆为主图（≤30 节点）+ 2~3 张子图，全部 12/12 通过；主图用双矩形子流程符号引用子图。

---

## M3 · LLM 自然语言建模（方向①）

**产出**：`llm-model.mjs`
- 输入：自由自然语言（如"画一个采购到付款流程，涉及采购员、采购经理、供应商、仓库、质检、应付会计、出纳，要能体现审批驳回和三单匹配"）
- 实现：system prompt 定义 **req.json JSON Schema** + few-shot（用 M1 模板做示例）→ 调 deepseek API（OpenAI 兼容）→ 返回 JSON → 结构校验（§4.1 协议）→ 落 req.json
- **降级链**：LLM 不可用/超时/JSON 非法 → 自动降级 `nlp-model.mjs`（DSL）→ 仍失败则给出"需要补充的信息清单"
- 输出：req.json + 置信度说明

**验收**：5 个自然语言用例（P2P / OTC / 入职 / 报销 / 请假审批）直接出图，全部 12/12；无 key 时降级链路可用。

---

## M4 · 整合与文档

1. **单命令智能体**：`agent-flow.mjs "自然语言需求"` → 建模（M3→降级链）→ 拆图（M2 按需）→ 渲染 → 验收 → 预览服务自动刷新
2. 规格文档 `flowchart-agent-spec.md` 更新（V4 深化完成、模板库、拆图规则、LLM 配置说明）+ HTML 重新生成
3. 端到端演示：3 个自然语言需求 → 在线预览

**验收**：一条命令从"一句话"到"多张可验收的图 + 在线预览"全链路跑通。

---

## 执行顺序与里程碑

```
M0 环境确认（需用户拍板）
  ↓
M1 知识库模板 ──┬─→ M2 自动拆图 ──┐
                └─→ M3 LLM 建模 ──┤
                                  ↓
                              M4 整合交付
```

## 风险与对策

| 风险 | 对策 |
|---|---|
| LLM API 不可用/限流 | 降级到 nlp-model（DSL）；模板兜底 |
| LLM 输出 JSON 非法 | schema 校验 + 重试 1 次 + 修复规则（缺字段补默认） |
| 拆图后子图语义断裂 | 子图保留自己的起止节点；主图折叠点用双矩形+编号，索引页互链 |
| 模板角色与需求不符 | finder 支持变量覆盖；LLM 模式可改模板 |
