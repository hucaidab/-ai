# 画布编辑器架构设计（v5-plan 批4 · 架构级，零技术债）

> 设计日期：2026-08-14
> 原则：架构/选型一次做对，不留技术债；妥协必须申报并记入 compromise-register.md
> 否决项：~~"拖拽+改文字最小可用版"~~（已否决——半吊子画布后续迭代全是技术债）

---

## 一、技术选型（零技术债决策）

| 选项 | 评估 | 结论 |
|------|------|------|
| **原生 SVG + 浏览器 ESM（复用现有模块）** | 零依赖、与全项目架构一致、可移植（openclaw/Claude Code 直接拷走）、preview-server 直接服务 | ✅ **采用** |
| React/Vue + 画布库（Konva/Fabric） | 交互代码少，但引入依赖+构建链+部署复杂度，违背项目零依赖原则，形成架构分叉 | ❌ 否决（本身就是技术债） |
| 后端 API 重渲染（每次操作请求服务器） | 实现快但慢、编辑器依赖服务器、拖拽实时性差 | ❌ 否决（体验债） |

**关键洞察**：现有 `parse-flow / layout-grid / render-svg / validate / req-util` 全是**纯 ESM、零 Node 依赖**——浏览器可直接 import 复用。编辑器交互层是唯一新代码，渲染/布局/验收零双实现。

## 二、架构与数据流（单一数据源）

```
editor.html（浏览器，type=module）
 ├─ import 现有模块（/lib/ 白名单路由服务）：
 │    parse-flow.mjs → layout-grid.mjs → render-svg.mjs → validate.mjs → req-util.mjs
 ├─ 新交互层（唯一新代码）：
 │    canvas-core：视口变换 / 命中检测 / 拖拽 / 选择框
 │    ops：编辑操作（拖拽/改文字/改样式/增删节点边/连边/撤销重做）
 │    操作栈：所有编辑 → {type, undo, redo} 入栈
 └─ 数据流（永不双写）：
      编辑操作 ──► req.json（内存对象，pos 字段记录手动位置）
        │
        ├─► render-svg 实时重渲染（拖拽中 60fps）
        ├─► 保存 ──POST /api/editor/save──► 写回磁盘 req.json + validate 验收 → 返回报告
        └─► 导出：复用 lib-export（PNG/PDF）
```

**req.json 协议扩展**（向后兼容，非破坏）：
```json
{ "id": "N1", ..., "pos": { "x": 320, "y": 180 } }   // 可选：手动位置
```
- 渲染器：节点有 `pos` → 用 pos（手动布局）；无 → 自动布局（现状不变）
- 语义：**自动布局与手动布局共存**，手动只覆盖位置，不破坏结构与样式

## 三、编辑操作全集（一次做全，不砍功能）

| 模块 | 操作 |
|------|------|
| **选择/视口** | 单击选择 / 框选 / 缩放（滚轮）/ 平移（拖空白）/ 适应画布 |
| **节点** | 拖拽移动（网格吸附 + 边随动重路由）/ 双击改文字 / 属性面板（形状切换·颜色·泳道·角色）/ 删除（连带边）/ 新增 |
| **边** | 从锚点拖出新边到目标 / 单击选中 / 双击改标签 / 改虚线（逆向）/ 删除 |
| **属性面板** | 选中节点：形状（rect/diamond/stadium/cylinder/subroutine/circle）、填充色、描边色、文本；选中边：标签、线型（实线/虚线）、颜色 |
| **撤销/重做** | 统一操作栈（Ctrl+Z / Ctrl+Y，按钮），覆盖全部编辑操作 |
| **主题** | 切换 4 主题（github-light/dark、enterprise-blue、bpmn），实时预览 |
| **保存/验收** | 保存 → 写回 req.json → validate 10 项验收 → 面板显示结果 |
| **导出** | PNG / PDF（复用 lib-export 服务端接口） |

## 四、新增/改动清单

| 文件 | 类型 | 内容 |
|------|------|------|
| `editor.html` | 新增 | 编辑器页面（UI：画布区+工具栏+属性面板+操作栈按钮） |
| `editor-core.mjs` | 新增 | 交互层：视口矩阵、命中检测、拖拽、操作栈、编辑器状态管理（浏览器 ESM） |
| `render-svg.mjs` | 小改 | 支持节点 `pos` 覆盖（自动布局与手动布局共存） |
| `preview-server.mjs` | 改 | ① 新增 `/lib/<name>` 白名单路由（仅 5 个核心模块，供浏览器 import）；② 新增 `/api/editor/save`（写 req.json + 验收）；③ **安全修复：/file/ 黑名单敏感文件（llm.config.json 等，现存漏洞一并修复）** |
| `compromise-register.md` | 维护 | 若实施中出现妥协 → 申报登记 |

## 五、验收标准（对应 v5-plan 批4）

1. 编辑后重渲染与手改一致（同一 req.json 双路径 diff）
2. req.json 回写无损（保存前后 JSON 结构 diff 验证）
3. 拖拽不重叠（网格吸附）
4. 撤销/重做覆盖全部操作类型
5. 保存后自动验收 10 项
6. 4 主题切换实时生效
7. 零妥协项（或全部已申报登记）

## 六、实施顺序

```
1. render-svg 支持 pos 覆盖（小，先做，编辑器依赖）
2. preview-server：/lib 白名单 + /api/editor/save + /file 安全修复
3. editor-core.mjs 交互层（视口/拖拽/选择/操作栈）
4. editor.html UI（画布/工具栏/属性面板）
5. 连边/属性/撤销全操作接入
6. 端到端验收 + 单测补充（pos 覆盖/保存往返）
```
